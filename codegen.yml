overwrite: true
schema: 'http://localhost:4000'
documents: null
generates:
  src/generated/index.ts:
    config:
      contextType: 'src/context#Context'
    plugins:
      - 'typescript'
      - 'typescript-resolvers'
